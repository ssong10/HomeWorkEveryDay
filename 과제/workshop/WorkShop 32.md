## WorkShop 32

```html
<script>
	let button = documents.querySelector('change-btn')
	let header = documents.querySelector('h1')
    let clickCount = 0
    button.addEventListener('click',funtion(event){
                            clickCount += 1
                            })
    header.innerText = `${clickCount}`
</script>
```

